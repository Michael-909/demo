import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const RunmapScreen = props => {
	return (
		<View style={styles.screen}>
			<Text>Run Map Screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	screen: {
		padding: 5
	},
});

export default RunmapScreen;